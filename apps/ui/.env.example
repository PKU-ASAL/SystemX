# SysArmor UI 宿主机开发环境配置
# 用于在宿主机上运行 pnpm dev 时连接后端服务

# API 连接配置 - 宿主机开发模式
NEXT_PUBLIC_API_HOST=localhost
NEXT_PUBLIC_API_PORT=8080
NEXT_PUBLIC_API_URL=/api/v1

# 开发模式标识
NODE_ENV=development

# 外部威胁情报 API 配置
NEXT_PUBLIC_THREAT_API_BASE_URL=http://110.40.136.112:1334/api
NEXT_PUBLIC_THREAT_API_ENABLED=true

# 注意：
# 1. 确保后端服务已通过 make deploy 启动
# 2. Manager API 应该在 http://localhost:8080 可访问
# 3. 如果后端运行在不同端口，请修改 NEXT_PUBLIC_API_PORT
