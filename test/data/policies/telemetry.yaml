# TelemetryPolicy only controls DataBatch boundaries.
meta: { id: tel-mvp, type: TELEMETRY, version: 1 }
target: { scope: host, selectors: { host: node-a } }
spec:
  telemetry:
    max_batch_items: 256
    max_batch_bytes: 262144
    flush_interval: 1s
