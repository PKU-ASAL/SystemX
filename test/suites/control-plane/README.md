# Control Plane Suite

Scope: `control`

System under test:

- `AgentControlPlaneService.Connect`;
- manager/agent mTLS identity;
- control stream sequence/replay semantics;
- control command, response, evidence, and session contracts.

Out of scope:

- endpoint collection effectiveness;
- policy/workload CPU benchmark;
- manager cloud incident scoring.
