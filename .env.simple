# =============================================================================
# SysArmor EDR 极简配置 - 只设置核心HOST和端口
# =============================================================================

# 部署模式
DEPLOYMENT_MODE=distributed
ENVIRONMENT=development

# =============================================================================
# 核心服务HOST配置 (只需要设置这4个)
# =============================================================================
MANAGER_HOST=localhost
MIDDLEWARE_HOST=49.232.13.155
PROCESSOR_HOST=localhost  
INDEXER_HOST=localhost

# =============================================================================
# 核心端口配置
# =============================================================================
MANAGER_PORT=8080
VECTOR_PORT=6000
KAFKA_PORT=9094
PROMETHEUS_PORT=9090
FLINK_PORT=8081
OPENSEARCH_PORT=9200

# =============================================================================
# 数据库配置 (最小必需)
# =============================================================================
POSTGRES_DB=sysarmor
POSTGRES_USER=sysarmor
POSTGRES_PASSWORD=password

# =============================================================================
# 自动派生的配置 (基于HOST)
# =============================================================================
# 这些可以在docker-compose.yml中自动派生
# KAFKA_BOOTSTRAP_SERVERS=${MIDDLEWARE_HOST}:${KAFKA_PORT}
# PROMETHEUS_URL=http://${MIDDLEWARE_HOST}:${PROMETHEUS_PORT}
# OPENSEARCH_URL=http://${INDEXER_HOST}:${OPENSEARCH_PORT}
# FLINK_JOBMANAGER_URL=http://${PROCESSOR_HOST}:${FLINK_PORT}
# WORKER_URLS=${MIDDLEWARE_HOST}:http://${MIDDLEWARE_HOST}:${VECTOR_PORT}:http://${MIDDLEWARE_HOST}:8686/health
